export const GamesPageInitialState = {
  games: [],
};

export const GamesPageReducer = (state = GamesPageInitialState, action: { type: any, payload: any}) => {

  switch (action.type) {

    default: return state;
  }
};
