import styled from 'styled-components/native';
import {Container} from '../../../../theme/components';

export const FilterGamesPageStyles  = {
  Container: styled(Container)`
  `,
  Item: styled.View`
    margin: 20px;
  `,
  Footer: styled.TouchableOpacity`
  `
};
